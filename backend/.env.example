# Server Configuration
PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/college_website

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_minimum_32_characters_long_change_this
JWT_EXPIRE=7d

# First Admin Configuration
# The first admin account can only be created with this email
# After first admin is created, only admins can create other admins
FIRST_ADMIN_EMAIL=admin@college.edu
